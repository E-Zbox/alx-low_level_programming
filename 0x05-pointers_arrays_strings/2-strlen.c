#include "string.h"
#include "main.h"

/**
 * _strlen - Return the length of a string
 * @s: string in array char type
 *
 * Return: int
 */
int _strlen(char *s)
{
	int stringSize = strlen(s);

	return stringSize;
}
